# Firebase Environment Variables
# For use with Firebase Hosting deployment

# API Configuration - When deployed to Firebase Hosting, use the local Functions endpoint
VITE_API_URL=/api

# Firebase Hosting settings
NODE_ENV=production
