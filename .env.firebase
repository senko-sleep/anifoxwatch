# Firebase Hosting — use when your API runs on Render (not the /api Cloud Function proxy).
# If you instead proxy via Firebase Functions, set VITE_API_URL=/api
VITE_API_URL=https://anifoxwatch-sm7s.onrender.com

NODE_ENV=production
