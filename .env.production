# Production API (baked in at `vite build`).
# Update this to your new Koyeb URL after deploying, then run:
#   npm run build && firebase deploy --only hosting
VITE_API_URL=https://anifoxwatch.vercel.app

# Optional secondary host — leave empty unless you have a fallback
# VITE_API_FALLBACK_URL=

# Shown on /health — same as primary
VITE_ANIWATCH_API_URL=https://anifoxwatch.vercel.app