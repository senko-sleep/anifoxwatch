# Production API (baked in at `vite build`). Browser calls this host directly.
# Primary: Cloudflare Workers (no cold starts, global edge, CORS always works).
# Fallback: Render (Puppeteer sources) — configured in api-config.ts.
VITE_API_URL=https://anifoxwatch-api.anifoxwatch.workers.dev
